FROM ubuntu:24.04

RUN apt-get update && apt-get install -y --no-install-recommends curl git sudo procps fzf zsh nano vim && apt-get clean && rm -rf /var/lib/apt/lists/*

RUN curl -fsSL https://claude.ai/install.sh | bash

COPY --chmod=755 claude-code/* /usr/local/bin/

ENTRYPOINT ["entrypoint"]
